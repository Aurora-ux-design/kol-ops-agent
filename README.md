# 达人运营智能 Agent

> 输入一个商品，Agent 告诉你应该找哪个达人、用什么脚本卖、佣金给到多少不亏、投放有没有异常。

面向品牌电商达人运营团队的 AI 决策辅助系统，覆盖达人运营全链路中四个核心决策环节——**选谁带货、怎么带货、赚不赚钱、投放值不值**。

---

## 为什么做这个项目

我做过 16 个月的快消品牌产品运营，最大的感受是：达人运营工作**重复度极高、但匹配效率极低**。同一批达人池，每次选品都要凭记忆和感觉重新想一遍"这次该找谁"；佣金谈判时手边没有实时的成本模型，只能大致估算保本线；投放开着之后基本就是"人肉盯盘"，达人在直播间到底有没有认真讲本品，品牌方其实并不知道。

市面上也有蝉圈圈这类工具，年费五六万，但用下来发现一个核心问题：**它只给一个匹配结果，不给理由，也不能调**。运营没法知道"为什么推荐这个达人"，更没法根据自己团队的经验去调整匹配逻辑——工具越黑盒，运营对它的信任度反而越低，很多时候还是回去凭手感做决策。

这个项目想验证的是：如果把"匹配理由可解释、匹配权重可调、关键决策留人工确认"这几件事做对，能不能让运营真正开始信任并依赖这套系统，而不是把它当摆设。

## 系统架构

四个引擎围绕一个决策闭环运转：

```
选匹配达人 → 生成脚本 → 计算损益 → 审核通过 → 达人开播 → 监控投产 → 数据回流
    ↑                                                              │
    └──────────────── 优化下次匹配的模型 ────────────────────────────┘
```

| 引擎 | 做什么 |
| --- | --- |
| 🎯 达人匹配引擎 | 向量粗排 + LLM 精排，商品↔达人双向匹配，附带可读匹配理由 |
| 📝 脚本生成引擎 | 适配达人风格的三形态脚本（口播/剧情/测评）+ 热点自动注入 |
| 💰 损益计算引擎 | 自然语言查询佣金上限、ROI、保本线——**计算过程不经过 LLM，全部确定性公式** |
| 🔍 投产审核引擎 | 异常检测 + 直播间品控校验（三重信号交叉验证达人是否真的在讲本品）|

四个引擎的调控参数都集中在统一的运营审核台里，凡是涉及资金或对外内容的环节，默认都需要人工确认后才生效。

完整设计细节（包括匹配权重、评估方法论、异常检测规则、飞书集成方案等）见 [`docs/PRD.md`](./docs/PRD.md)。

## 几个值得展开讲的设计判断

- **钱的计算不经过 LLM**：LLM 只负责"自然语言→结构化参数"和"数字→人话"，中间的算术部分全部走确定性 Python 公式，保证零误差。
- **品控校验用三重信号交叉验证，而不是让 LLM 直接判断"直播间在卖什么"**：截帧识别 + 橱窗扫描 + 口播关键词匹配，至少两路信号一致才出判定，单路不一致标记"存疑"要求人工核查——因为误判的代价（伤达人关系、触发自动暂停）远高于漏判。
- **自动止损默认关闭**：需要三路信号的历史准确率经过人工核查验证、达到可信阈值后，才逐步开放自动执行权限——从人工到自动化是渐进的，不是一步到位的。
- **匹配"准确率"拆成分层指标**，而不是只看最终 GMV：运营采纳率（即时）→ 达人配合率（短期）→ GMV/ROI 达标率（结算后）。GMV 结算周期长，且容易被达人体量本身掩盖真实的匹配质量，需要前置指标先判断系统是否在向好的方向迭代。

## 当前进度

- [x] 产品需求文档（V3.2，含四引擎详细设计、评估方法论、迭代路线图）
- [ ] 数据层 & 达人/商品模拟数据
- [ ] 损益计算引擎
- [ ] 达人匹配引擎
- [ ] 脚本生成引擎
- [ ] Streamlit 操作台
- [ ] 飞书消息推送

## 技术栈

Streamlit · ChromaDB · SQLite · Jinja2 · DeepSeek API · 飞书开放平台

## 路线图

详见 [`docs/PRD.md`](./docs/PRD.md) 第 11 节。V1.0 聚焦匹配 + 脚本 + 损益三个核心引擎的最小闭环；投产审核引擎的品控校验（技术难度最高的部分）规划在 V3.0。
