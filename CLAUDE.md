# CLAUDE.md

本文件为 Claude Code 在本仓库中工作的指引。完整产品设计见 [docs/PRD.md](docs/PRD.md)，本文件只覆盖工程层面需要遵守的约定。

## 项目背景

**达人运营智能 Agent** 是面向品牌电商达人运营团队的 AI 决策辅助系统，覆盖达人运营全链路四个核心决策环节：

- **选谁带货** —— 达人匹配引擎（向量粗排 + LLM 精排，商品↔达人双向匹配，附可读匹配理由）
- **怎么带货** —— 脚本生成引擎（口播/剧情/测评三形态，达人风格自适应 + 热点自动注入）
- **赚不赚钱** —— 损益计算引擎（自然语言查询佣金上限/ROI/保本线）
- **投放值不值** —— 投产审核引擎（异常检测 + 直播间品控三重信号交叉验证）

四个引擎通过决策闭环串联：`选匹配达人 → 生成脚本 → 计算损益 → 审核通过 → 达人开播 → 监控投产 → 数据回流`，回流数据用于优化下一次匹配。

**当前阶段**：V1.0 MVP 开发中，聚焦达人匹配 + 脚本生成 + 损益计算三引擎的最小闭环（投产审核引擎的品控校验规划在 V3.0，暂不实现）。

**核心设计原则，写代码前必须理解**：

1. **钱的计算不经过 LLM。** LLM 只做"自然语言 → 结构化参数"和"数字 → 人话"两端，中间算术全部走确定性 Python 函数，禁止让 LLM 输出金额/ROI/佣金率等数值本身。
2. **运营始终拥有最终拍板权。** 涉及资金（止损、佣金）或对外内容（脚本、热点）的环节，Agent 只产出候选和建议，默认需要人工确认后才生效——不要设计成自动执行。
3. **误判代价不对称，宁可"存疑"也不要武断判定。** 尤其是品控校验（V3.0）和自动止损类逻辑，单路信号不一致时应标记"存疑"交人工核查，而不是强行给出二元结论。
4. **可解释、可调，不做黑盒。** 匹配分数、脚本生成、异常检测都需要能输出"为什么"，权重/阈值/参数应可配置（读配置而非硬编码），呼应 README 里对"蝉圈圈只给结果不给理由"这类黑盒工具的反思。

## 技术栈

| 层级 | 选型 | 用途 |
| --- | --- | --- |
| Web 操作台 | Streamlit | 桌面端运营审核台，四引擎调控 UI |
| 移动端触达 | 飞书开放平台（Bot + 消息卡片） | 异常告警推送、待审核提醒、轻量审批 |
| 向量检索 | ChromaDB | 达人/商品 Embedding 粗排（语义相似度 Top N） |
| 结构化数据 | SQLite | 达人池、商品库、匹配记录、异常历史等持久化数据 |
| 脚本模板引擎 | Jinja2 | 脚本骨架模板（口播/剧情/测评）+ LLM 内容填充 |
| LLM 能力 | DeepSeek API（OpenAI 兼容接口） | 语义理解、多维精排、脚本生成、意图解析、结果人话化。原计划用 Claude API，因跨境支付/访问不便且中文语境场景下国内模型性价比更高，改用 DeepSeek |
| 外部数据 | CSV / ERP 导出 | MVP 阶段以 CSV 模拟真实数据源，逐步替换为直连 API |

不引入清单之外的框架（如另起一个 Web 框架、另一个向量库、另一个 ORM）。如确有必要，先在对话中说明理由，不要直接改动技术选型。

## 目录结构规范

```
kol-ops-agent/
├── app/                    # Streamlit 操作台入口与页面
│   ├── Home.py             # Streamlit 主入口
│   └── pages/               # 多页面（匹配参数/热点审核/损益参数/预警规则等标签页）
├── engines/                 # 四大引擎，每个引擎自成子包，互相之间只通过明确接口调用
│   ├── matching/            # 达人匹配引擎（粗排/精排/理由生成）
│   ├── script/               # 脚本生成引擎（风格分析/热点注入/模板填充/合规检查）
│   ├── profit/               # 损益计算引擎（确定性公式 + 自然语言查询）
│   └── audit/                 # 投产审核引擎（异常检测 + 品控校验，V2/V3 落地）
├── integrations/             # 外部系统适配层
│   ├── llm_client.py          # DeepSeek API 封装（统一走这里调用，不要在引擎代码里直接建 SDK 客户端）
│   ├── feishu.py              # 飞书消息推送/卡片审批
│   └── erp/                   # ERPDataProvider 抽象基类 + CSVDataProvider 等实现
├── data/                     # 数据层
│   ├── db.py                  # SQLite 连接与建表
│   ├── vector_store.py        # ChromaDB 封装
│   └── mock/                  # MVP 阶段的模拟 CSV 数据
├── templates/                 # Jinja2 脚本模板（按形态分目录：口播/剧情/测评）
├── config/                    # 运营可调控参数（匹配权重、预警阈值、合规词库等），非代码发布也能改的配置放这里
├── tests/                      # 测试，目录结构镜像 engines/ 和 data/
├── docs/
│   └── PRD.md
├── .env.example                # 环境变量样例（API Key 等，不含真实值）
└── CLAUDE.md
```

约定：

- **每个引擎是独立子包**，对外只暴露少数入口函数（如 `engines/matching/__init__.py` 导出 `match_influencers()`），内部实现细节不跨包直接引用。
- **Streamlit 页面（`app/`）不写业务逻辑**，只负责渲染和调用 `engines/` 暴露的函数；业务逻辑变化不应该要求改 UI 代码。
- **数据库表结构变更**统一放在 `data/db.py`（或未来的 migrations 目录），不要在业务代码里散落 `CREATE TABLE`。
- 运营可在后台调整的参数（权重、阈值、词库）放 `config/`，代码里读取配置而不是硬编码数字。

## 代码规范

**语言与风格**

- Python 3.11+，遵循 PEP 8，函数/变量用 `snake_case`，类用 `PascalCase`。
- 所有函数签名写类型标注（type hints）；跨模块调用的公共函数必须有类型标注，内部私有小函数可视情况从简。
- 不写文档字符串式的大段注释；仅在存在非显而易见的约束、坑点或权衡时加单行注释说明"为什么"，不写"做了什么"。
- 变量/字段命名优先英文；面向运营展示的文案（Streamlit UI 文本、飞书消息、脚本内容）用中文。

**LLM 调用**

- 所有 LLM 调用必须经过 `integrations/llm_client.py` 统一封装，便于统一做重试、超时、日志、模型/供应商切换——`create_message()` 的入参和返回结构仿照 Anthropic 的 tool-use 形状，是为了让业务代码（`nl_query.py`、`explain.py`）不需要感知背后到底是哪家 LLM。
- LLM 输出涉及结构化数据时用受限输出（如 JSON schema / 工具调用），不要依赖正则从自由文本里抠数据。
- 凡是"结果需要给运营看的数字"（金额、ROI、佣金率、权重分数汇总），必须能追溯到确定性计算函数，LLM 不直接生成这些数值。

**配置与密钥**

- API Key、飞书 Token 等一律从环境变量读取，通过 `.env`（不入库，已在 `.gitignore`）+ `.env.example`（入库，仅列出需要哪些变量，不含真实值）管理。
- 禁止把密钥、达人联系方式、真实佣金金额等敏感信息硬编码或提交到仓库。

**数据与外部依赖**

- ERP 数据访问统一通过 `ERPDataProvider` 抽象基类，MVP 阶段用 `CSVDataProvider` 实现；新增数据源新建子类，不要在业务逻辑里直接读文件。
- 涉及金额的计算函数保持纯函数（输入结构化参数，输出结构化结果），方便单测覆盖，不在计算函数内部做 I/O 或调用 LLM。

**测试**

- 损益计算引擎的公式函数、投产审核引擎的异常检测规则属于确定性逻辑，必须有单元测试覆盖边界条件（如佣金率恰好等于保本线）。
- 涉及 LLM/外部 API 的调用在测试中 mock 掉，不在测试里发真实请求。

**提交前检查**

- 新增/修改代码后运行 `python -m pytest`（测试配置见 `pyproject.toml`）；lint 工具尚未选定，待引入后在本文件补充命令。
- 涉及数据库表结构或配置文件格式变更时，同步检查 `config/` 下相关默认配置是否需要一并更新。

## 暂不需要做的事

- 不要提前实现 V3.0 的品控校验（视觉识别/ASR）、千川 API 直连、自动止损执行——当前阶段用不到，PRD 已注明分期。
- 不要在 MVP 阶段搭建多租户/权限分级，PRD 里团队协作功能是 V4.0 远期规划。
